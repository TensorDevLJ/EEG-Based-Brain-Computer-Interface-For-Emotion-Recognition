{
  "name": "internpe-task2",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npx concurrently \"python -m uvicorn backend.app:app --host 127.0.0.1 --port 8000\" \"npm --prefix frontend run dev\""
  }
}
